import type {TConfig} from './types';

const config: TConfig = {
  // apiRoot: 'http://localhost:9000',
  apiRoot: 'https://api.hermeez.co/v1',
  uiRoot: 'http://localhost:2007',
  debug: true,
  offlineMode: false
};

export default config;
