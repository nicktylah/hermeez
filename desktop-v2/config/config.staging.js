import type {TConfig} from './types';

const config: TConfig = {
  apiRoot: 'https://stg-api.hermeez.co/v1',
  uiRoot: 'https://stg-app.hermeez.co',
  debug: true,
  offlineMode: false
};

export default config;
