import type {TConfig} from './types';

const config: TConfig = {
  apiRoot: 'https://api.hermeez.co/v1',
  uiRoot: 'https://app.hermeez.co',
  debug: false,
  offlineMode: false
};

export default config;
